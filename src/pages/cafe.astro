---
import { getCollection } from "astro:content";
import GreetingTitle from "../components/GreetingTitle.svelte";
import PageHeader from "../components/PageHeader.astro";
import PlaylistCard from "../components/PlaylistCard.astro";
import PlaylistItemCard from "../components/PlaylistItemCard.astro";
import Layout from "../layouts/Layout.astro";
import { playlists, morePlaylists } from "../lib/data";

const storyEntries = await getCollection("stories");
---

<Layout title="Cafe">
  <div
    id="playlist-container"
    class="relative transition-all duration-1000 bg-context"
    style="min-height:300px;--context-color:#134e4a;"
  >
    <PageHeader type="cafe" />
    <h1 class="hover:rotate-180">JOJO Cafe</h1>
     community
    <br />
     Current Coffee

    <br />
     Where we source our coffee

    <br />
     Coffee Menu

    <br />
     Our Coffee Projects
    <br />
    <!-- <h1 class="text-4xl font-bold text-white">Cafe</h1>
    <div class="relative z-10 px-6">
      <GreetingTitle client:visible />
      <div
        class="grid gap-y-4 gap-x-6 sm:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 mt-6"
      >
        {
          storyEntries.map((story) => (
            <PlaylistItemCard
              playlist={{
                title: story.data.title,
                cover: story.data.cover,
                color: "#134e4a",
                slug: story.slug,
              }}
            />
          ))
        }
      </div>
    </div>
    <div
      class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80"
    >
    </div> -->
  </div>
  <div class="px-6 relative z-10 mt-4">
    <div class="self-center">In Construction</div>
    <!-- <h2 class="text-2xl font-bold">Made for you</h2>
    <div class="flex flex-wrap mt-6 gap-4">
      {morePlaylists.map((playlist) => <PlaylistCard playlist={playlist} />)}
    </div> -->
  </div>
</Layout>
